enum APIRoutes {
  USERS = '/api/users',
}

enum NavRoutes {
  HOME = '/',
  RULES = '/rules',
  EVENTS = '/events',
  PRICE_LIST = '/price-list',
  MODS = '/mods',
  WORLD = '/world',
}

export { APIRoutes, NavRoutes };
