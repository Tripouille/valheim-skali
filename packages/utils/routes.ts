enum APIRoutes {
  USERS = '/api/users',
}

enum NavRoutes {
  HOME = '/',
  RULES = '/rules',
  EVENTS = '/events',
  TRADE = '/trade',
  MODS = '/mods',
  WORLD = '/world',
}

export { APIRoutes, NavRoutes };
